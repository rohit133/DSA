function TwoPointer() {
    // Initialise the pointers. Generally, 
    // we start with either left = 0 and right = 0 OR left = 0 and right = len - 1;

    let left = 0;
    let right = 0 || len - 1;  


    // We should stop iterating till : left < right OR right < len - 1;
    while(condition) {
    
        if(condition) {
            // Update the pointer 
        }
    }


}