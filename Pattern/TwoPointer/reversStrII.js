const  sawap = (a,b) => {
    let temp = a
    a = b 
    b = temp;
}

/**
 * @param {string} s
 * @param {number} k
 * @return {string}
 */
var reverseStr = function(s, k) {
    s.split("");
    for(let i=; i<s.length; i++){
    }
};

console.log(reverseStr('abcdefg', 2));